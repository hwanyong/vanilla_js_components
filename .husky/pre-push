#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "Running pre-push checks..."

# Temporarily skip tests until we add them
echo "Skipping tests (temporarily disabled)..."

echo "All pre-push checks passed!"